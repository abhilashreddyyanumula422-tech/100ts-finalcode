/**
 * Deprecated — the single-scroll dashboard was split into separate portal pages:
 *   AgentOverview · AgentRequests · AgentVisits · AgentDelivery · AgentActivity
 * Kept only so any stale import keeps working. Safe to delete.
 */
export { default } from "./AgentOverview";
